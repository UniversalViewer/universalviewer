// todo: highlight, zoom, and rotation are not generic
enum Params {
    collectionIndex,
    manifestIndex,
    sequenceIndex,
    canvasIndex,
    zoom,
    rotation,
    highlight
}

export = Params;