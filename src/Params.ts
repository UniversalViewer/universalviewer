
// do not change order, index is important!
enum Params {
    collectionIndex,
    manifestIndex,
    sequenceIndex,
    canvasIndex,
    xywh,
    rotation,
    highlight,
    anchor
}

export = Params;