export class Events {
    static namespace: string = 'ebookExtension.';

    static LOADED: string                             = Events.namespace + 'loaded';
    static READER_CHANGED: string                     = Events.namespace + 'readerChanged';
}