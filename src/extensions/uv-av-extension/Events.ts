export class Events {
    static namespace: string = 'avExtension.';

    static RANGE_CHANGED: string                                = Events.namespace + 'rangeChanged';
    static TREE_NODE_SELECTED: string                           = Events.namespace + 'treeNodeSelected';
}