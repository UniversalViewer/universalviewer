define(function() {
    var paths = {
        'pdf': 'pdf_combined',
        'pdfobject': 'pdfobject'
    };

    return paths;

    var Dependencies = (function () {
        function Dependencies() {
        }
        return Dependencies;
    })();
    exports.Dependencies = Dependencies;
});
