{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "command": "grunt",
    "tasks": [
        {
            "label": "default",
            "type": "grunt",
            "task": "default",
            "problemMatcher": [
                "$lessCompile",
                "$tsc",
                "$jshint"
            ],
            "group": "build"
        }
    ]
}